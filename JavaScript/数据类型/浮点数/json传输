
1. 浮点型数据的传输后的iOS解析 直接转换成数值,不论是float 还是double都会有精度的变化. 那么我们在传递过程中就要规定数值保留小数点几位来完整表达数值的精确？
    而移动端一般只是进行显示,最好的处理方式就是 传递字符串. 
        这样不论 安卓还是iOS 都不会有处理上的不同.
    let data = {
        "bigDecimalNumber": 71.20,
        "bigDecimalString":"71.20"
    };
    IOS端解析：
        原值 71.20
        解析后 71.199997

    有关浮点型数据，后台传字符串的格式，防止丢失精度！！！