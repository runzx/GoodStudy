面向对象3大特性： 继承性

1. 避免复杂、重复定义

2. python 多个父类

3. 调用父类 ,2方法
    super(父类名, self).__init__(name,age)     #这个最好
    Human.__init__(self,name,age)           # 这儿要加self !

    __init__    如果子类定义了， 会自动重写了父类的__init__方法。
                父类的构造方法在子类中是不会被自动调用的，需要子类专门去调用父类的构造方法。这样才能将父类正确初始化。
                