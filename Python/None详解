None 空 
    类型： NoneType

1. 不同于
    空字符串， 空的列表， 0， Fales
2. 判空
    if not a        #这方式相对好！
    if a is None
3. None Fales
    None 表示不存在
    Fales 表示真假
4. 对象存在不一定是True
    2个方法决定
        __len__(self)     return 0      对象就成Fales
        __bool__(self)    return Fales  对象就成Fales  ,优先于__len__(有__bool__时， 不再运行__len__)