python 项目组织结构

1.  包       模块   类   函数/变量
    目录      文件  
2. 命名空间
    包名.模块名 ： 目录名.文件名            #文件名不用带.py
    包.子包
    包： 目录下有特定文件：__init__.py     #(这个也是模块 名字：包名)
3. 引用模块
    import 模块名
        简写： import 包.模块名 as 别名
    模块下变量： 模块名.变量
        简写： 别名.变量

    from module import  a,b     # 导入变量， 可直接使用a,b ；用*可以导入全部变量
                            模块中加 __ALL__=['a','b'] 来指定*引入的变量
    from 包 import 模块名
4.  vscode显示隐藏
    "files.exclude"
5. 一次导入多个模块
    多时可用 行尾加'\'换行， 也可以用(a,b,c,...)换行，
6. __init__.py 被导入时自动执行！
    可以把包和模块的初始化放这里面。
    通过 __ALL__=['模块名','...'] 控制 *时导入的模块
    import 模块， 其它的不用再次批量导入相应模块， 只用import 包
7. 包和模块不会被重复导入！
    要避免循环导入

    导入模块时会执行模块的代码。
8. 模块内置变量
    dir() 返回当前模块里有哪些变量：
        __name__        包.模块名       #入口文件的为 '__main__'
        __package__     包              #入口文件的为 空
        __doc__         注解，放在模块最前面，用''' '''包起
        __file__        完全路径，物理路径  #入口文件的为 当前文件（相对路径！）
        __