
多行字符串， 用'\'结尾， ES5扩展了字符串字面量的语法
    \ LineTerminatorSequence
    LineTerminatorSequence 指的是下面这些字符中的一个:
        换行符	    <LF>
        回车符	    <CR>
        行分割符	<LS>
        段分隔符	<PS>

    let tmp = '这是一个多行字符串测试， 这是第一行\
                这是第二行\
                这是第三行，加一个换行符<br>\
                这第四行。';
    
    当输出字符串时,换行符是不会显示出来的.如果你需要真实的换行,必须在反斜杠前面加上一个\n. 
