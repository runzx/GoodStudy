server服务器扩展点：

  'onPreStart' - 在启动连接侦听器connection listeners 之前调用。
  'onPostStart' - 在连接侦听器启动后调用。
  'onPreStop' - 在连接侦听器停止之前调用。
  'onPostStop' - 在连接侦听器停止后调用。使用新的url解析器

服务器不重启或关闭，上面事件只会激活一次！
  request 事件 不会激活上面事件
