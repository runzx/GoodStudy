
1. 登录 飞印中心1.0 获取以下3个飞印API集成必需的参数：
    - 商户编码（ member_code ）
    - API密钥（ API_KEY ）
    - 飞印虚拟打印机的设备编码（ deviceNo ），每个用户帐号可以有一个虚拟飞印打印机，对应有1个设备编码（一般为9字开头）。你在编写API集成代码时将需要用到此编码，以便把来自网站/业务系统的信息打印到此虚拟飞印打印机。
    
    飞印API将校验来自您的网站或应用服务器为每个打印请求生成的时间戳，对比飞印云平台服务器的时钟。从发送到收到之间的时延超过60秒的请求将被视为超时，飞印API将拒绝接收此类请求，并返回 -5 状态码—— 请求超时的错误提示。
        校准自身的时钟：125.90.88.37，210.72.145.44，ntp1.cs.mu.OZ.AU， ntp0.cs.mu.OZ.AU，ntp.alaska.edu。

2. 发送打印数据
    http://my.feyin.net/api/sendMsg
    POST 方法
    参数：
        reqTime	    是	请求（发生）时间戳，数值型	本次API请求发生的时刻，值为自 1970-1-1 0:0 到现在的 毫秒数 类似于Java语言中的 System.currentTimeMillis() 函数的取值。
        securityCode	是	安全校验码，字符串	调用飞印API的网站/应用系统用 API_KEY 针对请求内容生成的MD5哈希字串，此字串将被飞印API用于请求有效性的验证。 请参考 使用前的准备 了解如何获取 API_KEY 。
        memberCode	是	商户编码，数值型	您在 飞印中心 注册的帐号对应的（系统自动生成的）商户编码，可登录飞印中心按指引获取。
        deviceNo	是	飞印打印机的设备编码，字符串	打印请求将发送到对应此设备编码的飞印无线打印机。可在 飞印中心 “我的终端”中获取已注册到飞印中心的飞印打印机设备编码，也可通过以下方式直接从打印机本身获取：按一下通电中的飞印打印机背面黑色小按钮，即可打印出一张测试页面，其中包含本机设备编码。
        mode	    是	发送打印信息的格式类型，数值型	1 表示格式化信息，2 表示自由格式（自定义格式）信息。这种方法方便您灵活配置和测试打印信息格式，开发时建议通过一台测试机测试实际打印效果，直到获得满意的输出信息格式化效果。
        msgDetail	是	打印的数据/信息内容，字符串	对于 mode=1 的信息，打印输出的数据将按表格下方备注方式排版，当 mode=2 时，用户可自由设置打印格式，空行或空格等全部由调用程序自行控制。


0. 打印机输出宽度
    对于热敏票据打印机，输出宽度一般为58mm和80mm格式。
        58mm：每行宽度32个英文字符（半角单位），16个中文字符（全角单位）
        80mm：每行宽度48个英文字符，24个中文字符
        Tips:一般排版做文字间隔符时，可以采用横线“----”或者“====”等任意醒目的ASCII字符做间隔。
    左右对齐
        由于票据、标签打印机的打印宽度较小，为了具有最好的灵活性，我们建议程序员采用字符串左侧补空格（*\-等均可）、字符串填充和自动补齐等代码，配合对内容的定长进行额外控制的语句，以针对业务实现最好的打印效果。
    回车换行
        在正文中设置自然回车或者“\n”，打印内容都会自动换行，代码中需要采用“\n”的写法。
    打印一维条码
        <Barcode# Type=x Height=y>此处为目标条码的编码</Barcode#>
        Type 用于选择条码类型，取值范围：1 ≤ x ≤ 6，Type对应关系如下:
            1: UPC-A
            2: JAN13(EAN13)
            3: JAN8(EAN8)
            4: CODE39
            5: ITF
            6: CODABAR
        参数 Height 用于选择条码高度，取值范围: y={10, 15, 20, 25, 30}, 单位:mm。
        目标条码的编码为ASCII码格式，个数限制及字符集参照下面的 支持的条码类型。
    字体放大加粗
        字体长宽都支持最大8倍的放大，并且长宽放大倍数可以自由组合。
        <Font# Bold=x Width=y Height=z>此处为目标打印内容</Font#>
            说明：
            （1）参数Bold用于选择是否加粗，取值范围：x={0,1}
                其中0代表不加粗，1代表加粗
            （2）参数Width用于选择字体宽度，取值范围：1≤y≤8
    打印二维条码
        <QRcode# Size=10>http://www.feyin.net/</QRcode#>
        Size表示二维码的大小，Size的取值范围是：1<=size<=16中的任意正整数。
        二维码输入内容即QRCode标签中输入的长度<100个字符，

