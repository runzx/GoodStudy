python变量命名

1. 首字符
    不能是数字
2. 只能用字母、数字、下划线

3. 保留关键字 不能用

4. 变量不需要定义！
    int 值类型 str tuple (不可变)
    list 引用类型 （它的值是可改变）set dict
    能用元组的地方就不要用列表， 因为元组定义后就不可变！

5. 运算符
    //  整除
    %   余数
    **  幂运算

    赋值运算符       = += *= /= %= **= //= 
    关系运算符（比较） == ！= > < >= <=  
    逻辑运算
        int float 0 是 False, 非0是True
        字符串 ： 空字符串 是 False
        列表： [] False
        in , not in  字符串、元组、列表、集合。 字典 是针对key
